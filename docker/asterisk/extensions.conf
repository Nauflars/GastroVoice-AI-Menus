[default]
exten => _X.,1,NoOp(Incoming call to GastroVoice AI)

[gastrovoice]
exten => _X.,1,NoOp(GastroVoice AI Voice Assistant)
 same => n,Answer()
 same => n,AGI(gastrovoice.sh,${CALLERID(num)})
 same => n,Hangup()
