controllers:
    resource:
        path: '../src/'
        namespace: App
    type: attribute

when@dev:
    _errors:
        resource: '@FrameworkBundle/Resources/config/routing/errors.xml'
        prefix: /_error
