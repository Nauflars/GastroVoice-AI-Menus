services:
  backend:
    build:
      context: .
      dockerfile: docker/backend/Dockerfile
      target: development
    volumes:
      - ./backend:/var/www/html
      - ./docker/backend/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./docker/backend/supervisord.conf:/etc/supervisord.conf:ro
    ports:
      - "18080:8080"
    environment:
      APP_ENV: dev
      APP_DEBUG: "1"
      DATABASE_URL: "postgresql://${POSTGRES_USER:-gastrovoice}:${POSTGRES_PASSWORD:-gastrovoice}@db:5432/${POSTGRES_DB:-gastrovoice}?serverVersion=16&charset=utf8"
      REDIS_URL: "redis://redis:6379"
      JWT_SECRET_KEY: /var/www/html/config/jwt/private.pem
      JWT_PUBLIC_KEY: /var/www/html/config/jwt/public.pem
      JWT_PASSPHRASE: ${JWT_PASSPHRASE:-gastrovoice_jwt_passphrase}
      JWT_TOKEN_TTL: "3600"
      MESSENGER_TRANSPORT_DSN: "redis://redis:6379/messages"
      CORS_ALLOW_ORIGIN: "^https?://(localhost|127\\.0\\.0\\.1)(:[0-9]+)?$$"
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      OPENAI_MENU_MODEL: ${OPENAI_MENU_MODEL:-gpt-4.1}
      OPENAI_INTENT_MODEL: ${OPENAI_INTENT_MODEL:-gpt-4.1-mini}
      OPENAI_REALTIME_MODEL: ${OPENAI_REALTIME_MODEL:-gpt-realtime-2}
      OPENAI_TTS_MODEL: ${OPENAI_TTS_MODEL:-tts-1}
      OPENAI_TTS_VOICE: ${OPENAI_TTS_VOICE:-alloy}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - gastrovoice
    command: ["/bin/sh", "-c", "cd /var/www/html && composer install --no-interaction && /usr/bin/supervisord -c /etc/supervisord.conf"]

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: development
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "15173:5173"
    environment:
      VITE_API_URL: "http://localhost:18080"
    depends_on:
      - backend
    networks:
      - gastrovoice

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-gastrovoice}
      POSTGRES_USER: ${POSTGRES_USER:-gastrovoice}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-gastrovoice}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-gastrovoice}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - gastrovoice

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - gastrovoice

  asterisk:
    build:
      context: .
      dockerfile: docker/asterisk/Dockerfile
    profiles:
      - telephony
    volumes:
      - ./docker/asterisk/pjsip.conf:/etc/asterisk/pjsip.conf:ro
      - ./docker/asterisk/extensions.conf:/etc/asterisk/extensions.conf:ro
      - ./docker/asterisk/rtp.conf:/etc/asterisk/rtp.conf:ro
      - ./docker/asterisk/agi-bin:/var/lib/asterisk/agi-bin:ro
    ports:
      - "5061:5060/udp"
      - "5061:5060/tcp"
      - "10000-10020:10000-10020/udp"
    environment:
      GASTROVOICE_API: "http://backend:8080"
      RESTAURANT_ID: "${RESTAURANT_ID:-a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11}"
    depends_on:
      - backend
      - media-bridge
    networks:
      - gastrovoice

  media-bridge:
    build:
      context: docker/media-bridge
      dockerfile: Dockerfile
    profiles:
      - telephony
    ports:
      - "8765:8765"
      - "9093:9093"
    environment:
      GASTROVOICE_API_URL: "http://backend:8080"
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      OPENAI_REALTIME_MODEL: ${OPENAI_REALTIME_MODEL:-gpt-realtime-2}
      DEFAULT_VOICE: ${OPENAI_TTS_VOICE:-sage}
      DEFAULT_RESTAURANT_ID: "${RESTAURANT_ID:-a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11}"
      DEFAULT_RESTAURANT_NAME: "${RESTAURANT_NAME:-GastroVoice Demo}"
    depends_on:
      - backend
    networks:
      - gastrovoice

  mailpit:
    image: axllent/mailpit:latest
    ports:
      - "1025:1025"
      - "18025:8025"
    networks:
      - gastrovoice

volumes:
  postgres_data:
  redis_data:

networks:
  gastrovoice:
    driver: bridge
